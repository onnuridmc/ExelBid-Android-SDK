# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx2048m"

# Performance optimizations
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Android optimizations
android.enableJetifier=true
android.useAndroidX=true
# 앱 모듈이 exelbid-sample-common의 리소스를 자기 R 클래스로 참조하므로 전이 R 클래스 사용
android.nonTransitiveRClass=false
android.nonFinalResIds=true

# exelbid-sample-admob-nextgen이 요구하는 compileSdk 35는 AGP 8.2.2 공식 지원(34) 초과 —
# 동일 조합으로 빌드 검증됨. AGP 업그레이드 시 이 라인을 제거할 것.
android.suppressUnsupportedCompileSdk=35

# Build optimizations  
kotlin.code.style=official
kotlin.incremental=true
kotlin.incremental.android=true
